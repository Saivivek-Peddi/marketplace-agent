name: pizza
# Disabled by request — manual dispatch only, and all steps are no-ops.
on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pizza agent disabled"
